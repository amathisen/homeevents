location
  id
  name
  address

event
  id
  location_id (fk)
  date
  title

object_type
  id
  name
  base_table_name

user
  id
  name

activity_result
  id
  name
  highest_wins (bool)

activity
  id
  name
  description
  activity_result_id (fk)

notes
  id
  user_id (fk)
  date
  object_type_id (fk)
  object_id
  note_text

event_activities
  id
  name
  event_id (fk)
  activity_id (fk)

event_users
  id
  event_id (fk)
  user_id (fk)
  
event_activities_results
  id
  event_activities_id (fk)
  user_id (fk)
  activity_result_id (fk)
  result_value